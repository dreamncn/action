name:  ParallelsDesktopCrack

on:
  workflow_dispatch:

  schedule:
    - cron: '10 0/2 * * *'

jobs:
  ParallelsDesktopCrack:
    name: ParallelsDesktopCrack
    runs-on: ubuntu-latest
    steps:
      - name: clone remote git
        run: |
            git clone https://notabug.org/somebasj/ParallelsDesktopCrack.git 
            cd ParallelsDesktopCrack 
            git checkout remotes/origin/main
          
            git push https://${{secrets.GITHUB_TOKEN}}@github.com/dreamncn/ParallelsDesktopCrack.git
        
        
